server:
  port: 8080
  servlet:
    context-path: /cellphones

spring:
  application:
    name: cellphones_service

  datasource:
    url: jdbc:mysql://localhost:3306/cellphones_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # auto create/update table
    show-sql: true
    properties:
      hibernate:
        format_sql: true

debug: true
